解题思路：
可以只遍历i，然后j、k使用双指针进行确定。复杂度转为n^2.
假设nums[i]是三元组里最小的数。我们需要遍历所有可能的nums[i]。将数组排序后，可得遍历范围为[0-len-3]
然后假设中间数nums[j], 最大数nums[k], 使用left right双指针进行查找。left = i + 1, right = len - 1;
利用数组有序的性质，不断收缩left、right，直到找到目标i、j、k

需要注意的是无论是i、j、k，在滑动的过程中都需要跳过重复值。

etc：剪枝：三个大于0的数的和不可能等于0 nums[i] > 0 return res;
