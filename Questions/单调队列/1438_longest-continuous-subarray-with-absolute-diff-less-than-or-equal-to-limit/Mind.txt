解题思路：
子数组按以nums[L]起始分类讨论子解
if ([l, r]) 为nums[l]对应的解。
对于L = l + 1， R = [l+1..r]时 一定满足【数组中的任意两个元素之间的绝对差必须小于或者等于 limit 】，因为此时范围变小了，肯定更容易达成该条件。
因此只需寻找first r make [l+1..r] 不满足，此时其对应的R = r - 1.

子数组中两两绝对差的最大值即为Max - Min, 用单调队列维护。

