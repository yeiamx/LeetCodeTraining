解题思路：
整体思路是把问题转成一个数组的最大累加和问题

我们将所有的矩阵分类，按仅包含i-j行的方式
例：一共有4行，那么矩阵可能包含[0-1]行[0-2]行[0-3]行[1-2]行[1-3]行[2-3]行
换句话说，如果我们能求出有且仅有[i-j]行的矩阵的最大累加和即可

当我们定下了哪些行后，矩阵的累加和就只在j方向上产生变化，这和数组其实一样，我们只需把矩阵压缩一下即可

Solution中的代码在计算压缩nums时进行了重复累加，可以优化


