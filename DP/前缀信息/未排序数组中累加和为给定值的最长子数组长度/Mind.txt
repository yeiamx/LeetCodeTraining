解题思路：
子数组分类成以nums[i]结尾的子数组，我们只需遍历i，求出以nums[i]结尾的子数组的【累加和为给定值的 最长子数组长度】，然后取最大值即可

可以把问题抽象成如下
【pre】【..i， sum=aim】 从i向左找最远的子数组使得数组和为aim
等同于求前缀和最早为preSum[i + 1]-aim是什么时候
【pre， sum=preSum[i + 1] - aim】【..i】,
可以用Hash表记录，不用每次都算

必须要记录map[0] = -1;
意思为数组为空时，前缀和为0.
用以应对求前缀和最早为0时的下标。